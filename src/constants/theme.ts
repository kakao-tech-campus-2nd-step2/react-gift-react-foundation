export type ThemeName = "apple" | "orange" | "mango" | "banana" | "melon";

export const ThemeColor = {
  apple: "#ff0000",
  orange: "#ffa500",
  mango: "#ffd700",
  banana: "#ffdf00",
  melon: "#ffcccb",
} as const;
